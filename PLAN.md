# PLAN

## 目标

实现一个不依赖 ROS 2、相机或硬件 SDK 的机器人迎宾业务模块。

输入 `Event`，由业务状态机根据当前状态生成本次事件产生的 `Effect`。

## 实施步骤

1. 在 `robot_application/models.py` 中定义不可变的 `Event` 和 `Effect` 数据类。
2. 在 `robot_application/application.py` 中实现 `RobotApplication`。
3. 维护当前在场人员、离场时间、迎宾去重、送客去重、对话状态和会议状态。
4. 实现 `PERSON_ENTERED` 的迎宾逻辑。
5. 实现 `PERSON_LEFT` 的离场记录逻辑，不立即送客。
6. 实现 `TICK` 的 10 秒超时检查和一次性送客逻辑。
7. 保证对话或会议期间不产生迎宾、送客和机器人动作。
8. 实现 `snapshot()`，返回内部状态的隔离副本。
9. 在 `tests/` 中使用 pytest 编写测试。
10. 编写 `ARCHITECTURE.md`、`REPORT.md` 和 `AI_USAGE.md`。
11. 执行测试并记录真实测试结果。

## 状态设计

`RobotApplication` 负责拥有以下状态：

- 当前在场人员；
- 每个人的离场时间；
- 已经迎宾的人员；
- 已经完成送客的人员；
- 当前是否处于对话；
- 当前是否处于会议。

## 主要业务规则

- 首次进入且当前不在对话或会议中时，输出挥手和“欢迎光临”。
- 同一次持续在场期间不重复迎宾。
- 离开时只记录时间，不立即送客。
- 离开满 10 秒后，由 `TICK` 触发一次送客。
- 10 秒内返回时取消本次送客，不重复迎宾。
- 完成送客后再次进入，视为新的接待过程。
- 对话或会议期间不产生迎宾、送客或机器人动作，结束后不补发。

## 测试计划

至少覆盖：

- 首次进入和重复进入；
- 对话期间抑制迎宾；
- 会议期间抑制迎宾；
- 离场未满 10 秒不送客；
- 离场满 10 秒送客一次；
- 多次 `TICK` 不重复送客；
- 短暂离开后返回；
- 完成离场确认后再次进入；
- `snapshot()` 返回值与内部状态隔离。

## 架构边界

业务模块只负责状态管理和生成 `Effect`，不直接调用 ROS 2、相机或硬件 SDK。

未来通过独立的 bridge 或 adapter 将 `Effect` 转换成 ROS 2 action、service 或导航请求。
